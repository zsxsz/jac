{
  "name": "jac",
  "version": "1.0.0",
  "description": "Java Blackjack Game",
  "scripts": {
    "build": "mkdir -p dist && cp -r public/* dist/ && chmod +x build.sh && ./build.sh",
    "start": "java -cp dist Blackjack"
  },
  "dependencies": {
    "serve": "^14.0.0"
  }
}
